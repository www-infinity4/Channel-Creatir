name: Validate Channel Creatir
on:
  push:
    branches: [main]
  pull_request:
permissions:
  contents: read
jobs:
  syntax:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Check creator JavaScript syntax
        run: node --check app.js
      - name: Verify required files
        run: |
          test -s index.html
          test -s styles.css
          test -s README.md
          test -f .nojekyll
